javascript:(function () {    let calculatorVisible = false;    let commandUIVisible = false;    let panelUIVisible = false;    let calculatorFollowingMouse = false;    const calculator = createCalculator();    const infoUI = createInfoUI('Press [, for the calculator to open and close, and press / for more commands!');    const commandUI = createCommandUI('Commands');    const panelUI = createPanelUI();    document.body.appendChild(calculator);    document.body.appendChild(infoUI);    document.body.appendChild(commandUI);    document.body.appendChild(panelUI);    fadeInElement(infoUI);    setTimeout(() => {        fadeOutElement(infoUI);    }, 5000);    document.addEventListener('keydown', (event) => {        if (event.key === '[') {            toggleCalculator();        } else if (event.key === '/') {            toggleCommandUI();        } else if (event.key === ';') {            togglePanelUI();        } else if (event.key === 'Control') {            toggleCalculatorFollowingMouse();        }    });    function createCalculator() {        const calculator = document.createElement('div');        calculator.style.position = 'fixed';        calculator.style.top = '50%';        calculator.style.left = '50%';        calculator.style.transform = 'translate(-50%, -50%) scale(0)';        calculator.style.border = '1px solid #ccc';        calculator.style.borderRadius = '8px';        calculator.style.padding = '10px';        calculator.style.backgroundColor = 'gray';        calculator.style.width = '300px';        calculator.style.zIndex = '9999';        calculator.style.userSelect = 'none';        calculator.style.display = 'none';        calculator.style.transition = 'transform 0.3s ease-in-out, opacity 0.2s ease-in-out';        const display = document.createElement('input');        display.type = 'text';        display.style.width = '100%';        display.style.marginBottom = '10px';        display.readOnly = true;        const buttonsContainer = document.createElement('div');        buttonsContainer.style.display = 'grid';        buttonsContainer.style.gridTemplateColumns = 'repeat(4, 1fr)';        buttonsContainer.style.gridGap = '5px';        const buttons = [            '7', '8', '9', '/',            '4', '5', '6', '*',            '1', '2', '3', '-',            '0', '.', '=', '+',            '(', ')', '^', '√',            'π', 'e', 'sin', 'cos',            'tan', 'log', 'ln', '!',            'x²', 'x³', '1/x', '|x|',            '1/n', 'n²', 'n³', 'C',            '1 1/n'        ];        buttons.forEach(button => {            const btn = createCalculatorButton(button);            buttonsContainer.appendChild(btn);        });        const createdByText = document.createElement('div');        createdByText.textContent = 'If you have any problems, message mrcat.gtag on Discord.';        createdByText.style.color = 'black';        createdByText.style.fontSize = '10px';        createdByText.style.textAlign = 'center';        createdByText.style.marginTop = '10px';        calculator.appendChild(display);        calculator.appendChild(buttonsContainer);        calculator.appendChild(createdByText);        return calculator;    }    function createPanelUI() {        const panelUI = document.createElement('div');        panelUI.style.position = 'fixed';        panelUI.style.top = '50%';        panelUI.style.right = '0';        panelUI.style.transform = 'translateX(100%)';        panelUI.style.padding = '10px';        panelUI.style.backgroundColor = 'rgba(128, 128, 128, 0.75)';        panelUI.style.color = 'white';        panelUI.style.width = '200px';        panelUI.style.zIndex = '9997';        panelUI.style.userSelect = 'none';        panelUI.style.transition = 'opacity 0.3s ease-in-out';        const resizeText = document.createElement('p');        resizeText.textContent = 'Resize calculator';        resizeText.style.color = 'white';        resizeText.style.textAlign = 'center';        const resizeSlider = document.createElement('input');        resizeSlider.type = 'range';        resizeSlider.min = '0';        resizeSlider.max = '2';        resizeSlider.step = '0.1';        resizeSlider.value = '1';        resizeSlider.style.width = '100%';        const moreText = document.createElement('p');        moreText.textContent = 'More coming soon!';        moreText.style.color = 'white';        moreText.style.textAlign = 'center';        moreText.style.marginTop = '10px';        panelUI.appendChild(resizeText);        panelUI.appendChild(resizeSlider);        panelUI.appendChild(moreText);        resizeSlider.addEventListener('input', () => {            const scaleFactor = resizeSlider.value;            calculator.style.transform = %60scale(${scaleFactor})%60;        });        return panelUI;    }    function togglePanelUI() {        if (panelUIVisible) {            panelUI.style.transform = 'translateX(100%)';            panelUI.style.opacity = '0';            setTimeout(() => {                panelUI.style.display = 'none';            }, 300);        } else {            panelUI.style.display = 'block';            setTimeout(() => {                panelUI.style.transform = 'translateX(0)';                panelUI.style.opacity = '1';            }, 0);        }        panelUIVisible = !panelUIVisible;    }    function createInfoUI(message) {        const infoUI = document.createElement('div');        infoUI.style.position = 'fixed';        infoUI.style.top = '10px';        infoUI.style.left = '50%';        infoUI.style.transform = 'translateX(-50%)';        infoUI.style.padding = '10px';        infoUI.style.backgroundColor = 'rgba(128, 128, 128, 0.75)';        infoUI.style.borderRadius = '5px';        infoUI.style.color = 'white';        infoUI.style.textAlign = 'center';        infoUI.style.fontSize = '12px';        infoUI.style.zIndex = '9999';        infoUI.style.display = 'none';        infoUI.style.transition = 'transform 0.3s ease-in-out, opacity 0.2s ease-in-out';        const infoText = document.createElement('p');        infoText.textContent = message;        infoUI.appendChild(infoText);        return infoUI;    }    function createCommandUI(title) {        const commandUI = document.createElement('div');        commandUI.style.position = 'fixed';        commandUI.style.top = '50%';        commandUI.style.left = '0';        commandUI.style.transform = 'translateX(-100%)';        commandUI.style.padding = '10px';        commandUI.style.backgroundColor = 'black';        commandUI.style.color = 'white';        commandUI.style.width = '200px';        commandUI.style.zIndex = '9998';        commandUI.style.userSelect = 'none';        commandUI.style.transition = 'transform 0.3s ease-in-out';        const commandTitle = document.createElement('div');        commandTitle.textContent = title;        commandTitle.style.fontSize = '14px';        commandTitle.style.fontWeight = 'bold';        commandTitle.style.marginBottom = '10px';        const controlPanelText = document.createElement('p');        controlPanelText.textContent = 'Press ; to open a control panel';        controlPanelText.style.color = 'white';        controlPanelText.style.marginTop = '10px';        commandUI.appendChild(commandTitle);        commandUI.appendChild(controlPanelText);        return commandUI;    }    function toggleCommandUI() {        if (commandUIVisible) {            slideOutElement(commandUI);        } else {            slideInElement(commandUI);        }        commandUIVisible = !commandUIVisible;    }    function fadeInElement(element) {        element.style.transform = 'translate(-50%, -50%) scale(1)';        element.style.opacity = '1';        element.style.display = 'block';    }    function fadeOutElement(element) {        element.style.transform = 'translate(-50%, -50%) scale(0)';        element.style.opacity = '0';        setTimeout(() => {            element.style.display = 'none';        }, 200);    }    function slideInElement(element) {        element.style.transform = 'translateX(0)';    }    function slideOutElement(element) {        element.style.transform = 'translateX(-100%)';    }    function toggleCalculator() {        if (calculatorVisible) {            fadeOutElement(calculator);        } else {            fadeInElement(calculator);        }        calculatorVisible = !calculatorVisible;    }    function toggleCalculatorFollowingMouse() {        if (calculatorFollowingMouse) {            fadeOutElement(commandUI);            document.removeEventListener('mousemove', moveCalculatorWithMouse);        } else {            slideInElement(commandUI);            document.addEventListener('mousemove', moveCalculatorWithMouse);        }        calculatorFollowingMouse = !calculatorFollowingMouse;    }    function moveCalculatorWithMouse(event) {        calculator.style.left = %60${event.clientX}px%60;        calculator.style.top = %60${event.clientY}px%60;    }    function createCalculatorButton(button) {        const btn = document.createElement('button');        btn.textContent = button;        btn.style.padding = '15px';        btn.style.fontSize = '12px';        btn.style.cursor = 'pointer';        btn.addEventListener('click', () => addToDisplay(button));        return btn;    }    function addToDisplay(value) {        const display = calculator.querySelector('input[type="text"]');        if (value === '=') {            try {                display.value = eval(display.value);            } catch (error) {                display.value = 'Error';            }        } else if (value === 'C') {            display.value = '';        } else {            if (value === '1/n') {                const numerator = prompt('Enter the numerator:');                const denominator = prompt('Enter the denominator:');                if (numerator && denominator) {                    display.value += %60(${numerator}/${denominator})%60;                }            } else if (value === '1 1/n') {                const wholeNumber = prompt('Enter the whole number:');                const numerator = prompt('Enter the numerator:');                const denominator = prompt('Enter the denominator:');                if (wholeNumber && numerator && denominator) {                    display.value += %60(${wholeNumber} ${numerator}/${denominator})%60;                }            } else {                display.value += value;            }        }    }    function showCalculator() {        fadeInElement(calculator);    }    function hideCalculator() {        fadeOutElement(calculator);    }})();
